"""
Small sanity tests for the strategy modules. Nothing fancy -- just enough
to catch obvious breakage (e.g. build_prompt returning empty string, or a
strategy crashing on an empty behavior).

TODO: add a test per strategy once its build_prompt/run is implemented.
"""
